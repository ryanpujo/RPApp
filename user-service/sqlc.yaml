version: "2"
sql:
  - engine: "postgresql"
    queries: "./sql/query/users.sql"
    schema: "./sql/schema/schema.sql"
    gen:
      go:
        package: "repository"
        out: "repository"
        emit_json_tags: true
